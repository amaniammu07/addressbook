pipeline {
    agent any
    
    stages {
      stage('git pull source code') {
         steps {
            git branch: 'master' ,url: 'https://github.com/amaniammu07/addressbook.git'
              }
            }
            stage('hello') {
              steps {
                 echo 'helloworld
                 }
             }
          }
       }
